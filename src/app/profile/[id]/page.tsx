import { UserProfilePage } from '@/screens/profile';
import { Header } from '@/shared/ui/Header';

export default function Page() {
    return (
        <div>
            <Header />
            <UserProfilePage />
        </div>
    );
}
